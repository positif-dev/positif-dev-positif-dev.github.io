import React, { FC } from 'react'
import './footer.scss'

interface IProps {
  ClassName?: string
}

export const Footer: FC<IProps> = (props) => {

  return (
    <footer className = 'footer'>
      
    </footer>
  )
}